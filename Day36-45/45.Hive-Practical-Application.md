# Hive Practical Application

## Hive Overview

[Hive](https://hive.apache.org/) is a data warehouse tool created by Facebook, based on Hadoop, and maintained by Apache. It's the most widely applied big data solution. Simply, Hive converts SQL queries to MapReduce or Spark tasks, providing perfect SQL support for convenient big data statistics. It enables people unfamiliar with Java/Scala to work with big data platforms and distributed file systems, implementing data storage and processing.

Hive's main functions:

1. Map structured data files to tables.
2. Provide HiveQL (SQL-like query language) for large-scale data operations.
3. Depend on Hadoop's HDFS storage and MapReduce/Spark/Tez execution engines.

Hive characteristics:

1. **SQL-like syntax**: Supports SQL queries, lowering big data learning barriers.
2. **Scalability**: Handles PB-level data relatively easily.
3. **Flexible storage**: Supports multiple formats (Text, ORC, Parquet, Avro, etc.).
4. **Selectable execution engines**: MapReduce, Tez, Spark.
5. **Higher latency**: Suited for offline analysis, not low-latency OLTP queries.

Hive vs. traditional relational databases: Hive excels in batch processing massive data volumes but lacks the real-time query capabilities of traditional databases.

## Environment Setup

### Preparation (Ubuntu Example)

1. Sync timezone:
   ```bash
   sudo timedatectl set-timezone Asia/Shanghai
   ```

2. Change package sources:
   ```bash
   sudo apt update
   sudo apt upgrade
   ```

3. Install essential tools:
   ```bash
   sudo apt install -y vim wget net-tools openssh-server
   ```

4. Start SSH:
   ```bash
   sudo systemctl start ssh
   sudo systemctl enable ssh
   ```

5. Configure firewall:
   ```bash
   sudo ufw enable
   sudo ufw allow ssh
   ```

6. Prepare Hadoop:
   ```bash
   wget https://archive.apache.org/dist/hadoop/common/stable2/hadoop-2.10.2.tar.gz
   sudo tar -zxvf hadoop-2.10.2.tar.gz -C /opt
   ```

7. Prepare Hive:
   ```bash
   wget https://archive.apache.org/dist/hive/stable-2/apache-hive-2.3.9-bin.tar.gz
   sudo tar -zxvf apache-hive-2.3.9-bin.tar.gz -C /opt
   ```

### Java Environment

Check Java version:
```bash
java -version
```

Install OpenJDK 8:
```bash
sudo apt install -y openjdk-8-jdk
```

### MySQL Environment

Install MySQL:
```bash
sudo apt install mysql-server
```

Configure:
```bash
mysql_secure_installation
```

Enable and start:
```bash
sudo systemctl enable mysql
sudo systemctl start mysql
```

### Launch Environment

Start HDFS:
```bash
start-dfs.sh
start-yarn.sh
```

Start Hive:
```bash
hive --service metastore &
hive --service hiveserver2 > hiveserver2.log 2> hiveerrors.log &
```

Check ports:
```bash
ss -ntl
```

## Common HDFS Commands

| Command | Purpose |
|---------|---------|
| `hadoop fs -ls <path>` | List directory contents |
| `hadoop fs -ls -R <path>` | Recursively list all files |
| `hadoop fs -du <path>` | Show file/directory size |
| `hadoop fs -mkdir <path>` | Create directory |
| `hadoop fs -rm <path>` | Delete file |
| `hadoop fs -copyFromLocal <local> <hdfs>` | Copy file to HDFS |
| `hadoop fs -copyToLocal <hdfs> <local>` | Copy file from HDFS |
| `hadoop fs -cat <path>` | Output file contents |
| `hadoop fs -tail <path>` | Show file end contents |
| `hadoop fs -cp <src> <dst>` | Copy within HDFS |
| `hadoop fs -mv <src> <dst>` | Move/rename within HDFS |

## Basic Hive Syntax

Drop database:
```hive
DROP DATABASE IF EXISTS eshop CASCADE;
```

Create database:
```hive
CREATE DATABASE IF NOT EXISTS eshop;
```

Switch context:
```hive
USE eshop;
```

Create external table:
```hive
CREATE EXTERNAL TABLE IF NOT EXISTS dim_user_info 
(
user_id           STRING,
user_name         STRING, 
sex               STRING,
age               INT,
city              STRING,
first_active_time STRING,
level             INT,
extra1            STRING,
extra2            MAP<STRING, STRING>
)
ROW FORMAT DELIMITED 
FIELDS TERMINATED BY '\t'
COLLECTION ITEMS TERMINATED BY ','
MAP KEYS TERMINATED BY ':'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE;
```

Load data:
```hive
LOAD DATA LOCAL INPATH '/home/hadoop/data/user_info/user_info.txt' OVERWRITE INTO TABLE dim_user_info;
```

Create partitioned table:
```hive
CREATE TABLE IF NOT EXISTS fact_user_trade 
(
user_name      STRING,
piece          INT,
price          DOUBLE,
pay_amount     DOUBLE,
goods_category STRING,
pay_time       BIGINT
)  
PARTITIONED BY (dt STRING)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\t'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE;
```

Set dynamic partitions:
```hive
SET hive.exec.dynamic.partition=true;
SET hive.exec.dynamic.partition.mode=nonstrict;
SET hive.exec.max.dynamic.partitions=1000;
SET hive.exec.max.dynamic.partitions.pernode=10000;
```

Hive enables SQL-based big data analysis without requiring Java/Scala expertise, making it accessible for data analysts and business intelligence professionals.
